import { theme } from "./admin";
import { theme as clientTheme } from "./client";
import { theme as publicTheme } from "./public";
export const themes = {
  admin: theme,
  client: clientTheme,
  public: publicTheme,
};
